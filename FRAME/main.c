//Run this code on each individual computer, having it route in and run the program, outputting the results to a text file, then deleting it in the main.py
#include <stdio.h>
int main()
{
   // printf() displays the string inside quotation
   printf("===============================")
   printf("Checking Status of Computers");
   return 0;
}